# Internal Findings
- 2026-09-09 Minutes: ConversationMinutesClient uses separate full-transcript text request (Gemini Interactions/OpenAI Responses). CallRecord stores conversationMinutes/minutesStatus/minutesModel independently; transcript saved first, report failure never overwrites it. AppState.minutesJobs guards duplicates; manual retry requires no audio. No implicit cloud for Apple: minutesProvider must explicitly select cloud and corresponding env key must be opted in. Structured points require exact source quote matches, not a semantic correctness guarantee.
- 2026-09-09 Model selection: AppSettings persists geminiTranscriptionModel/openAITranscriptionModel independently; absent fields retain prior defaults. Gemini accepts user model ID; OpenAI limited to diarize/whisper-1 for segment timestamps. CloudTranscription.makeRequest validates before upload and selects model-specific format.
- 2026-09-09 Releases: CI derives bundle SUPublicEDKey from SPARKLE_KEY via generate_keys import into ephemeral runner Keychain; generate_appcast may exit 0 despite key mismatch. Require edSignature and sign_update verification before draft creation. v1.0.1 draft has unsigned enclosure; do not publish it.
- 2026-09-09 SIP credentials: SIPSettings encodes no password; legacy decode only for SettingsStore migration. Keychain service de.limtec.siptray.sip/account primary-account; write Keychain before sanitized JSON; failed load blocks saves until successful reload. Scripts/test_settings.sh uses injected in-memory store, not real credentials.
- 2026-09-09 Licenses: Scripts/copy_licenses.sh copies exact dependency notices to Resources/Licenses in local/CI packaging. Project license remains unselected. build.sh/CI disable G.722.1 via --disable-g7221-codec; Package.swift excludes g7221codec; G.722 stays enabled. Stamp g7221-disabled-v1 and versioned CI cache invalidate old builds.
- 2026-09-09 Transcription: AppState per-record jobs; never recover active calls. Cloud mixes synchronous mono WAV tracks via ConversationAudio into one full AAC/M4A (silence preserved, shorter track padded); one request, no client-side chunking. Gemini/OpenAI diarize label speakers; Whisper has no speaker labels. Reject oversized uploads explicitly; raw recordings retained for retry under seven-day cleanup. Apple path remains per-track.
- 2026-09-08 Settings: Sources/Models.swift decodeIfPresent required for added fields; synthesized decoding previously discarded whole settings when fields absent. Provider defaults Apple; cloud opt-in, keys read from ~/.env, never persist imported keys.
- 2026-09-08 Apple Speech: Sources/CallTranscriptionService.swift serializes callbacks on main queue; timeout/error no longer promotes partial text to completed transcript. Removed duplicate onset offset added to file-relative timestamps.
- 2026-09-09 | Shared permission assistant: identical `PermissionAssistant.swift`, `PermissionWizardProgress.swift`, `NSApplication+Extensions.swift` across DockAppToggler (`Features/Permissions`, `Core/Extensions`), SnapCapture (`Sources/SnapCapture/Permissions`), SipTray (`Sources/Permissions`). Keep all copies synchronized. Async consent blocks advancement until callback; closed wizard retains pending startup callbacks; one final restart only for steps requiring it. SipTray `Scripts/test_permissions.sh` exercises actual AppKit window with fake grants, no TCC prompts.
- 2026-09-09 | SipTray permissions: `SipPermissionRequester` only native consent entry point. Startup `AppState.checkPermissions` gates SIP start: microphone → speech only when Apple transcription enabled. Settings/provider changes/microphone test/recovery use same assistant; `CallTranscriptionService` never prompts. Denied grants stay in wizard; grant callbacks resume service/recovery. No AX/input/screen permissions needed.
